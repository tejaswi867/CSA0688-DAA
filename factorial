#include<stdio.h>
int main()
{
	int fact=1,i=1,num;
	printf("enter:");
	scanf("%d",&num);
	if(num<0)
	printf("input is invalid");
	else
	{
	for(i=2;i<=num;i++)
	{
		fact=fact*i;
	}
	printf("the factorial of %d is %d",num,fact);
}
	}
